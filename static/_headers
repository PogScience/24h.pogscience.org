# Cloudflare Pages Headers Configuration

# Adds Content-Type header to iCal export, as Zola
# is only capable of exporting HTML files as of now.
/streams-ics*
  Content-Type: text/calendar; charset=utf-8

# Disables indexing on pages.dev domain (just in case)
https://24h-pogscience-org.pages.dev/*
  X-Robots-Tag: noindex
